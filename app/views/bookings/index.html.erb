<h1>Your bookings</h1>

<div class="container">

  <div class="row">
      <% @current_user.bookings.each do |booking| %>
    <div class="col-12 col-lg-8">
      <%= cl_image_tag booking.car.image, height: 300, width: 400, crop: :fill %>


    </div>

    <div class="col-12 col-lg-4">
      <h3> <%= booking.car.name%> </h3>
      <p> <%= booking.car.start_date %></p>
      <strong><%= booking.car.price %>€/día</strong>
      <p><%= booking.car.end_date %></p>
      <%= button_to "Cancel my book", booking_path(booking), :data => { :confirm => "Are you sure?" }, :method => :delete, class: "btn btn-default btn-xs" %>



    </div>
    <% end %>

  </div>
</div>



